<Window x:Class="MineLauncher.JavaBrowser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:properties="clr-namespace:MineLauncher.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Strings.JavaBrowser_Title}" Height="250" Width="400"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin="4" SelectionMode="Single"
                  MouseDoubleClick="ListViewDoubleClicked"
                  ItemsSource="{Binding JavaPaths}"
                  SelectedItem="{Binding SelectedPath, Mode=TwoWay}"/>
        <Button Grid.Row="1" Grid.Column="0" Command="{Binding OkCommand}" MinWidth="50" Margin="4" Padding="4, 2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{x:Static properties:Strings.Dialog_Ok}"/>
        </Button>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding CancelCommand}" MinWidth="50" Margin="4" Padding="4, 2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{x:Static properties:Strings.Dialog_Cancel}"/>
        </Button>
    </Grid>
</Window>
